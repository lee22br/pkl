amends "Application.pkl"

hostname = "localhost"

port = 3599

environment = "stage"

database {
  host = "localhost"
  port = 5786
  username = "admin"
  password = read?("env:DATABASE_PASSWORD") ?? "1234"
  dbName = "myapp"
}