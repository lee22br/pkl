amends "Application.pkl"

hostname = "localhost"

port = 8080

environment = "demo"

database {
  host = "localhost"
  port = 5786
  username = "admin"
  password = read?("env:DATABASE_PASSWORD") ?? "1234"
  dbName = "myapp"
}